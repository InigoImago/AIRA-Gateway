{
  "name": "aira-e2e",
  "private": true,
  "license": "Apache-2.0",
  "description": "Browser-driven end-to-end tests for the AIRA stack (Playwright).",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit",
    "format:check": "prettier --check \"**/*.ts\""
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.0.0",
    "prettier": "^3.8.1",
    "typescript": "~5.9.0"
  }
}
