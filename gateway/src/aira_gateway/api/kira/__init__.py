"""The KIRA compatibility surface: the predecessor's wire contract (`FRD-107`, `ADR-0010`)."""

#: Where the surface is mounted, so a migrating consumer changes a base URL and nothing else.
BASE = "/kira/api/external"
